# Compiled binary
/spot

# OAuth refresh token cache — contains a long-lived credential, NEVER commit
.spotify-cache

# Local memory store (per-user listening events, feedback, notes)
spot.db
spot.db-shm
spot.db-wal

# Local env files
.env
.env.*

# macOS metadata
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Go
vendor/
/.claude/
